apply plugin: 'ratpack'

buildscript {
  repositories {
    mavenCentral()
  }
  dependencies {
    classpath 'com.augusttechgroup:ratpack:0.6.2'
  }
}

dependencies {
  groovy 'org.codehaus.groovy:groovy:1.8.8'
}

task wrapper(type: Wrapper) {
  gradleVersion = '1.3'
}

